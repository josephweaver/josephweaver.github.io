## CLT in $\mathbb{R}^{d}$

Convergence theorem
" 881 continuity convergece theoren"
$Z_{n} \Rightarrow Z_{00} \quad$ iff $\quad \varphi_{Z_{n}}(t) \xrightarrow[n \rightarrow \infty]{ } \varphi_{Z_{00}}(t) \quad \forall t \in \mathbb{R}^{d}$
Cramer-WOID Device.
A
sweden $\quad$ Iturgey Prifat columbia.
Invent sey. States.
Invent sey. States.
$X_{n} \Rightarrow X_{\infty} \quad$ if $\quad t \cdot Z \Rightarrow t X_{\infty} \quad \forall t \in \mathbb{R}$.
Reduce Problem to 1 Dimension.
$\varphi_{t . x_{n}}(1) \longrightarrow \varphi_{t . x_{\infty}}^{(1)}$
Basic CLT
$\left\{x_{n}\right\}$ are iid in $\mathbb{R}^{d}, E(Z)=0, \begin{aligned} & \text { couprience } \\ & \text { marin. } \\ & (Z)\end{aligned}=\left[\Gamma_{i, j}\right]_{1 \leqslant i, j \leqslant \theta} \Gamma_{i, j}=E\left(\varepsilon_{i} \cdot \varepsilon_{j}\right)$
$\bar{Z}=\left(\begin{array}{c}\varepsilon_{i} \\ \vdots \\ \varepsilon_{0}\end{array}\right)$
$E\|Z\|^{2}=E \sum_{k=1}^{\partial} \varepsilon_{k}^{2}<\infty$
then. $\frac{\delta_{n}}{\sqrt{n}} \Rightarrow G$ where $G \sim N(\overrightarrow{0}, \Gamma(\bar{x}))$
where $\delta_{n}=\sum_{k=1}^{n} Z_{k}$
$G=A_{j \times j} \vec{Z}$

$$
\vec{z}=\left(\begin{array}{c}
z_{i} \\
\vdots \\
z_{J}
\end{array}\right)\left\{z_{k}\right\} \stackrel{i i d}{\sim} \quad v(0,1)
$$

cov. Nat of $G$.
$\Gamma(G)=E\left(A \cdot Z \cdot Z^{t} A^{t}\right)=A \cdot E\left(Z Z^{t}\right) A^{t}$

$$
\begin{aligned}
& =A \cdot \Gamma(z) A^{t} \\
& =A A^{t}
\end{aligned}
$$

$$
\Gamma(t)=A \cdot A^{t}
$$

why is there on A like this.
∴
(1) $\Gamma(E)$ is symmetric, $\Gamma(E)=\Gamma(Z)^{t}$
$a_{i, j}=a_{j, i}$
Also (2) Positive semidefinite. $\quad \Gamma(x) \succeq 0$
Def $\quad \vec{t}^{\top} \cdot \Gamma t \geq 0$
$t^{*} \sqcap t=\operatorname{Var}(t \Sigma)=E(t \cdot \Sigma)^{2}=E\left(t^{\prime} \overline{X X X}^{\prime} t\right)$
Also we an find Eigenvalues.
0 - orthogoal matric if $0 \cdot 0^{t}=I$
Eigendecomposition.

$$
\Gamma(x)=O D O^{t}
$$

where $D=\left(\begin{array}{cc}\lambda_{1} & 0 \\ 0 & 1 \\ 0 & \\ \lambda_{1}\end{array}\right)$ Dingoval metoic,

$$
\begin{aligned}
E & =\left(\begin{array}{llll}
\sqrt{\lambda} i & & & \\
& \sqrt{\lambda} & & \\
& & \ddots & \\
& & & \sqrt{N}
\end{array}\right) \quad \quad E \cdot E^{t}=D \\
O E(O E)^{t} & =A A^{t}
\end{aligned}
$$

why $G \sim N(0, \Gamma(z))$ is. $G=A \vec{z}$ have same Distrib.
wts: $\varphi_{s_{n}}(t) \longrightarrow \varphi_{G}(t), t \in \mathbb{R}^{d}$

$$
\varphi_{t \cdot S_{n}}(1) \rightarrow \varphi_{t G}(1)
$$

So whet : $t \cdot S_{n}$

$$
t \cdot s_{n}=\sum_{k=1}^{n} t \cdot x_{k}
$$

㕶。
$\frac{\sum_{k=1}^{n} t \cdot x_{k}}{\sqrt{n}} \Rightarrow N\left(0, \sigma_{t \neq}^{2}\right)$
where $\sigma_{t+x}^{2} \quad t^{\prime} \Gamma t$
(we Reduee to 1 dim throupt knomer-wold deffice therely use 881 tech
$\varphi_{t, G}^{(1)}=e_{\in \mathbb{R}^{2}}^{-\frac{t^{\prime} \Gamma t}{2}} \cdot x^{2}$ From Normell c.f.
CLT $\left\{\vec{X}_{n, k}^{E}\right\}_{1 \leq k \leq n}^{I N D} E\left(X_{n, k}\right)=\overrightarrow{0}, \operatorname{Cov}\left(X_{n, k}\right)=\Gamma_{n, k}$
$S_{n}=\sum_{k=1}^{n} x_{n, k}, \quad \Gamma\left(S_{n}\right)=\sum_{k=1}^{n} \Gamma_{n, k} \quad$ if (1) $\sum_{k=1}^{n} \Gamma_{n, k} \longrightarrow \Gamma$ coveresgece of matrix . Dof As All Element corverge.
(2) $L_{n}(\varepsilon)=\sum_{k=1}^{n} E\left(\left\|x_{n, k}\right\|^{2} ;\left\|x_{n, k}\right\|>E\right) \rightarrow 0 \quad \forall \varepsilon>0$
"Lindeburg condition"
$E\left\|X_{n, k}\right\|^{2}<\infty \quad 1 \leq k \leq n$,
then $S_{n} \Rightarrow N(\overrightarrow{0}, \Gamma)$
uses cauchy-swante inequellity →
if $G \sim N(M, \Pi)$ then $\varphi_{x}(t)=e^{i t \cdot \vec{\mu}} \cdot e^{-t^{\prime} \cdot \Gamma t / 2} \quad \forall t \in \mathbb{R}^{d}$ if integrate get densaty.

$$
\begin{aligned}
f_{G}(\vec{x})= & (2 \pi)^{d / 2} \hat{\exp }\left\{-\frac{1}{2} \cdot(\vec{x}-\vec{\mu})^{t} \cdot \Gamma^{-1}(\vec{x}-\bar{\mu})\right\} \quad \forall x \in \mathbb{R}^{\partial} \\
& \operatorname{Set}(\Gamma)^{-1 / 2}
\end{aligned}
$$

Multinomal with $\partial+1$ outcomes

$$
, X_{n} \sim M N\left(n, P_{n_{1}}, \ldots P_{n, 1}, q_{n}\right)
$$

vertor

$$
\text { Assure } n \cdot P_{n, k} \xrightarrow[n \rightarrow \infty]{ } \lambda_{k} \quad k=1, \ldots, \partial \text {. }
$$

vertor
Assure $n$. Prik $\underset{\cap \rightarrow \infty}{\longrightarrow} \lambda_{k} \quad k=1, \ldots, \partial$.
this implies $P_{n} . k \rightarrow 0 \quad ; \quad ; \quad q_{n} \rightarrow 1$
want $x$ coord with out $q_{n}$.

$$
\left(\begin{array}{c}
x_{n, 1} \\
x_{n, 2} \\
\vdots \\
x_{n, d} \\
x_{n, d+1}
\end{array}\right) \quad Y_{n}=\left(\begin{array}{c}
x_{n, 1} \\
\vdots \\
x_{n, d}
\end{array}\right)
$$

then

$$
\begin{aligned}
& y \Rightarrow\left(y^{(1)}, \ldots, y^{(d)}\right) \\
& \quad k=1, \ldots d_{1} \\
& \left\{y ^ { ( k ) } \left\{_{1 \leq k \leq \partial}\right.\right. \\
& Y^{(k)} \sim \operatorname{Poisson}(\lambda k)
\end{aligned}
$$

use this theoren all the time,

$$
\begin{aligned}
& \sum_{k=1}^{n}\left(1+a_{n, k}\right) \rightarrow e^{a} \\
& \sum_{k=1}^{n} a_{n, k} \underset{n \rightarrow \infty}{\longrightarrow} a
\end{aligned}
$$

The C.F. or the cont is the Prodol." Tost come obt" "becones Asyntoticallz indeperdul-

